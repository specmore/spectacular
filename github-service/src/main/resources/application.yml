server:
  port: 5000
github:
  api:
    app:
      id: ${GITHUB_APP_ID:testId}
      private-key-file-path: ${GITHUB_APP_PRIVATE_KEY_FILE_PATH:testPath}
    root-url: https://api.github.com
security:
  authentication:
    jwt:
      signature-secret: ${JWT_SECRET:this_test_shared_key_is_32_bytes}
#logging:
#  level:
#    org:
#      springframework:
#        security: DEBUG